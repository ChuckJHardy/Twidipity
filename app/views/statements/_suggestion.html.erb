<div class="grid-item">
    <%= image_tag(suggestion.profile_image_uri, alt: suggestion.screen_name, class: "grid-item-img") %>
    <div class="grid-item-name-wrapper">
      <a href="#" class="play-pause-btn pause-btn">Pause</a>
      <div class="grid-item-name">
        <%= link_to(suggestion.uri, target: :blank, class: "grid-item-link") do %>
          @<%= suggestion.screen_name %>
        <% end %>
      </div>
      <div class="grid-item-timer">
        <% if defined?(statement) %>
          <%= distance_of_time_in_words_to_now(statement.ending_at) %>
        <% end %>
      </div>
    </div>
  <% if defined?(statement) %>
    <%= link_to 'delete', statement_path(statement), method: :delete, :class => 'grid-delete-link' %>
  <% end %>
</div>